import { FormLogin } from '@/admin/login/components/FormLogin';

export default function AdminLoginPage() {
  return (
    <div className="flex min-h-screen items-center justify-center ">
      <FormLogin />
    </div>
  );
}
